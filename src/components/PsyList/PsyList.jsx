// import { useEffect, useState } from 'react';
import PsyCard from 'components/PsyCard';

const PsyList = () => {
  // [database, setDatabase] = useState([]);

  // useEffect(() => (database = firebase.database()));

  return <PsyCard />;
};

export default PsyList;
